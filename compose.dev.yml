version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: hantek-station
    ports:
      - "8000:8000"
      - "5173:5173"
    volumes:
      - ./server:/app
      - ./ui:/ui